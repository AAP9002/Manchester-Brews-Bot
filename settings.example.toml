CIRCUITPY_WIFI_SSID = ""
CIRCUITPY_WIFI_PASSWORD = ""

# Single Slack Workflow webhook — message content is templated in
# utils/config.SLACK_MESSAGES and dispatched via utils/slack.py.
SEND_MESSAGE_SLACK_WEBHOOK = ""

# Google Apps Script Web App URL — coffee log + count
GOOGLE_SHEET_URL = ""

# Person responsible for refilling beans; substituted into the
# Low-on-beans success message. Defaults to "Someone" if unset.
LOW_BEANS_PERSON = ""

WEATHER_LAT = ""
WEATHER_LON = ""
